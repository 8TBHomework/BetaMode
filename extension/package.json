{
  "name": "betamode",
  "scripts": {
    "build": "parcel build --no-content-hash --no-source-maps --no-cache --detailed-report 0",
    "watch": "parcel watch --no-cache --no-hmr"
  },
  "source": "src/manifest.json",
  "dependencies": {
    "webext-options-sync": "^3.0.0",
    "webextension-polyfill": "^0.8.0"
  },
  "devDependencies": {
    "@parcel/config-webextension": "^2.0.0",
    "@parcel/transformer-image": "^2.0.0",
    "npm-run-all": "^4.1.5",
    "parcel": "^2.0.0",
    "stylelint": "^14.0.1",
    "stylelint-config-xo": "^0.20.1",
    "xo": "^0.46.3"
  },
  "webExt": {
    "sourceDir": "dist"
  }
}
